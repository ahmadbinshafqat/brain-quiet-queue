# brain-quiet-queue
A tiny web app that turns a messy browser tab list into a calm, prioritized reading queue. Users paste links, and the app uses lightweight LLM summaries or heuristics to rank what to read next by estimated effort and relevance.
